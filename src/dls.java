import java.util.ArrayList;

public class dls {
//    function DEPTH-LIMITED-SEARCH(problem, l) returns a solution, or failure, or cutoff
//  frontier ← a FIFO queue initially containing one path, for the problem's initial state
// one path = 2 connected nodes? https://www.educative.io/answers/what-is-depth-limited-search
ArrayList<TNode> frontier = new ArrayList<TNode>();
//  solution ← failure
//  while frontier is not empty do
//    parent ← pop(frontier)
//    if depth(parent) > l then
//      solution ← cutoff
//    else
//      for child in successors(parent) do
//        if child is a goal then
//          return child
//        add child to frontier
//  return solution

}
